---
import "../styles/global.css";
import type { Metadata } from "../utils/seo";
import Skeleton from "./Skeleton.astro";

const { meta = {} }: { meta?: Partial<Metadata> } = Astro.props;
---

<Skeleton meta={meta}>
  <slot />
</Skeleton>
