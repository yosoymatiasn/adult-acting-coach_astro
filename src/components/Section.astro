---
const { children, className = "" } = Astro.props;
---

<div class={`${className} px-4 md:px-0`}>
  <div class="container py-10 flex flex-col justify-center items-center">
    {children}
  </div>
</div>
